@model IEnumerable<CategoryModel>
<div class="card">
    <ul class="list-group">
        @foreach (var item in Model)
        {
            <li class="list-group-item">
                <a href="#">@item.Name</a>
            </li>
        }
    </ul>
</div>